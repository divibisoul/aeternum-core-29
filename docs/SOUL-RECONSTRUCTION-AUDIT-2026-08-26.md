# SOUL Reconstruction Audit — 2026-08-26

## Purpose
This record is a reconciliation checkpoint. Claims of implementation are accepted only when a corresponding file, commit, test, or runtime evidence exists in the repository.

## False-positive corrections
1. A documentation-only contract or matrix is not an implementation of runtime connectivity.
2. An endpoint declaration is not proof of reachability or E2E execution.
3. A 60-channel matrix is topology/synergy intent, not 60 successful connections.
4. A passing APK compilation is not proof that Mesh, AI Session, capabilities, or Pilot work at runtime.
5. The Android MainActivity must retain the living event cockpit; administrator controls must be additive, not a replacement for system telemetry.

## Last-operation reconciliation
- `d31f5e9`: deployment evidence registry — documentation only; correctly marks remote peers as unverified.
- `30b32c9`: 60-channel synergy matrix — documentation only; correctly separates affinity from runtime proof.
- `ea0717f`: Android JVM target correction — real build fix; aligned Java/Kotlin targets at 17.
- `8795c45`: restored the living Android cockpit and retained administrator controls in the same UI. This prevents the admin screen from suppressing the original event bus/system telemetry surface.

## Required proof chain
For each directed channel:
request → selected transport → destination → capability resolution → real handler → correlated response → observable effect.

For system synergy:
verified channel traces → composed flows → parallel execution traces → measurable result.

## APK upgrade principle
The APK is the hybrid user/runtime boundary. It must expose the Cockpit/Pilot, Web Session, system state, and Mesh without pretending that unavailable Android privileges or hardware acceleration exist.

## Performance principle
Soul optimizes effective throughput through routing, concurrency, caching, reuse, and workload placement where the platform permits. It must not claim to increase CPU/GPU clock speed or RAM capacity merely by installation.

## Completion gate
Do not label the system production-ready until:
- Android build passes;
- instrumentation passes;
- all six runtimes have actual deployment endpoints or an explicitly supported local execution mode;
- all capabilities are inventoried against real handlers;
- 60 directed channel traces are executed and correlated;
- composed synergy scenarios pass;
- AI Web Session → Provider → Pilot is demonstrated in the APK;
- final Cockpit exposes the resulting system state and controls.
